){.devrow{grid-template-columns:1fr 1fr;gap:16px}.device{margin-top:0!important;transform:none!important}.devscreen:not(.has-photo) h3{min-height:0}}

/* ---- STATS ---- */
.stats{background:#050608;border-top:1px solid var(--line);padding:6vw 4vw;display:grid;grid-template-columns:repeat(4,1fr)}
.stat{padding:0 3vw 0 0;border-left:1px solid var(--line);padding-left:3vw}
.stat:first-child{border-left:none;padding-left:0}
.stat b{display:block;font-size:clamp(38px,5vw,68px);font-weight:800;letter-spacing:-.03em;color:var(--blue)}
.stat span{display:block;margin-top:10px;font:500 13px var(--sans);color:var(--dim)}
@media(max-width:860px){.stats{grid-template-columns:1fr 1fr;row-gap:36px}.stat{border-left:none;padding-left:0}.stat:nth-child(2n){padding-left:3vw;border-left:1px solid var(--line)}}

/* ---- EVIDENCE / STEPS ---- */
.evidence{padding:0;border-top:1px solid var(--line)}
.evidence-hero{position:relative;padding:9vw 4vw;overflow:hidden}
.evidence-hero:before{content:"";position:absolute;inset:0;
  background:url(/knowledge-center/taking-ideas-into-the-real-world/assets/ideas-into-reality-editorial.png) center/cover no-repeat;z-index:0}
.evidence-hero:after{content:"";position:absolute;inset:0;z-index:1;
  background:linear-gradient(90deg,rgba(0,0,0,.82) 0%,rgba(0,0,0,.2) 32%,rgba(0,0,0,.2) 66%,rgba(0,0,0,.85) 100%),
             linear-gradient(0deg,rgba(0,0,0,.5),rgba(0,0,0,.1) 40%)}
.evidence-head{position:relative;z-index:2;display:flex;justify-content:space-between;align-items:flex-end;gap:5vw;flex-wrap:wrap}
.evidence-head h2{margin:0;font-size:clamp(34px,4.6vw,64px);font-weight:800;letter-spacing:-.03em;max-width:10ch;
  text-shadow:0 4px 24px rgba(0,0,0,.5)}
.evidence-head p{margin:0;font:500 18px/1.55 var(--sans);color:#f4f3ee;max-width:30ch;
  text-shadow:0 2px 14px rgba(0,0,0,.65)}
.steps-wrap{padding:5vw 4vw 8vw}
@media(max-width:860px){
  .evidence-head{flex-direction:column;align-items:flex-start;gap:24px}
  .evidence-hero{padding:64px 20px}
}
.steps{display:grid;grid-template-columns:repeat(5,1fr);border-top:1px solid var(--line)}
.step{padding:28px 20px 0;border-left:1px solid var(--line)}
.step:first-child{border-left:none}
.step b{font:600 12px var(--sans);color:var(--blue)}
.step strong{display:block;margin:36px 0 10px;font-size:19px;font-weight:800;letter-spacing:-.02em}
.step p{margin:0;font:400 13px/1.5 var(--sans);color:var(--dim)}
@media(max-width:860px){.steps{grid-template-columns:1fr}.step{border-left:none;border-top:1px solid var(--line);padding:22px 0 0}.step:first-child{border-top:none}}

/* ---- LEADERSHIP ---- */
.leadership{padding:8vw 4vw;border-top:1px solid var(--line)}
.lead-kicker{font-family:var(--serif);font-style:italic;font-weight:400;font-size:clamp(38px,6vw,86px);color:var(--dim);margin:0}
.lead-kicker b{font-family:var(--sans);font-style:normal;font-weight:800;color:var(--fg)}
.lead-roster{margin-top:5vw;border-top:1px solid var(--line)}
.lead-row{display:flex;justify-content:space-between;align-items:baseline;padding:32px 0;border-bottom:1px solid var(--line);gap:24px;flex-wrap:wrap}
.lead-row h3{margin:0;font-size:clamp(28px,3.6vw,52px);font-weight:800;letter-spacing:-.03em}
.lead-row p{margin:6px 0 0;font:400 15px/1.5 var(--sans);color:var(--dim);max-width:48ch}
.lead-row span{font:600 12px var(--sans);letter-spacing:.04em;text-transform:uppercase;color:var(--dim);white-space:nowrap}
.lead-row:hover h3{color:var(--blue)}

/* ---- CONTACT / PING (Hobro treatment) ---- */
.contact{background:#000;padding:10vw 4vw 8vw;text-align:center;position:relative;overflow:hidden;border-top:1px solid var(--line)}
.arcs{position:absolute;top:50%;left:0;transform:translateY(-50%);display:flex;gap:16px;opacity:.5}
.arcs.right{left:auto;right:0}
.arcs svg{width:120px;height:220px}
.arcs path{stroke:#fff;fill:none;stroke-width:1}
.gotproject{font:600 20px var(--sans);color:#cfcfca;margin:0 0 18px}
.pingus{margin:0;font-size:clamp(48px,9.5vw,150px);font-weight:800;letter-spacing:-.04em;line-height:.9;white-space:nowrap}
.contact-cta{display:inline-flex;align-items:center;gap:10px;margin-top:38px;background:#fff;color:#000;
  padding:14px 26px;border-radius:99px;font:600 14px var(--sans)}
@media(max-width:860px){.arcs{display:none}}

/* ---- FOOTER ---- */
footer{padding:34px 4vw;display:flex;justify-content:space-between;align-items:center;font:500 12px var(--sans);color:var(--dim);border-top:1px solid var(--line)}
footer .flinks{display:flex;gap:24px}
@media(max-width:860px){footer{flex-direction:column;gap:14px;text-align:center}}

@media(prefers-reduced-motion:reduce){*{transition:none!important}}

/* ============ ANIMATION SYSTEM ============ */
.reveal{opacity:0;transform:translateY(28px);transition:opacity .8s cubic-bezier(.16,1,.3,1),transform .8s cubic-bezier(.16,1,.3,1)}
.reveal.in{opacity:1;transform:translateY(0)}
.stagger .reveal:nth-child(1){transition-delay:.02s}
.stagger .reveal:nth-child(2){transition-delay:.1s}
.stagger .reveal:nth-child(3){transition-delay:.18s}
.stagger .reveal:nth-child(4){transition-delay:.26s}
.stagger .reveal:nth-child(5){transition-delay:.34s}

.hero-line{overflow:hidden;display:block}
.hero-line span{display:block;transform:translateY(110%);transition:transform 1.1s cubic-bezier(.16,1,.3,1)}
.hero.loaded .hero-line span{transform:translateY(0)}
.hero.loaded .hero-line:nth-child(1) span{transition-delay:.15s}
.hero.loaded .hero-line:nth-child(2) span{transition-delay:.3s}
.herotop .eyebrow{opacity:0;transform:translateY(10px);transition:opacity .8s ease,transform .8s ease}
.hero.loaded .herotop .eyebrow{opacity:1;transform:translateY(0)}
.hero.loaded .hero-cta{opacity:1!important;transform:scale(1)!important}
.hero-cta{opacity:0;transform:scale(.85);transition:opacity .8s ease .6s,transform .8s cubic-bezier(.34,1.56,.64,1) .6s}
.hero-cta{animation:spin 26s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.hero.loaded .hero-cta{animation:spin 26s linear infinite}

/* signal ticker */
.sigwrap{border-top:1px solid var(--line);border-bottom:1px solid var(--line);background:#000;overflow:hidden;height:44px;display:flex;align-items:center}
.sigtrack{display:flex;white-space:nowrap;animation:tick 26s linear infinite}
.sigtrack span{font:600 12px var(--mono,monospace);letter-spacing:.14em;text-transform:uppercase;color:var(--dim);padding:0 26px;display:flex;align-items:center;gap:26px}
.sigtrack em{width:5px;height:5px;border-radius:50%;background:var(--blue);display:inline-block;font-style:normal}
@keyframes tick{to{transform:translateX(-50%)}}

/* count-up nudge */
.stat b{transition:opacity .5s ease}

/* device draw-in */
.devscreen svg path{stroke-dasharray:600;stroke-dashoffset:600;transition:stroke-dashoffset 1.4s cubic-bezier(.16,1,.3,1)}
.device.in .devscreen svg path{stroke-dashoffset:0}
.device{opacity:0;transform:translateY(30px) rotate(0deg)!important}
.device.in{opacity:1;transform:rotate(var(--r,0deg)) translateY(0)!important;transition:opacity .8s cubic-bezier(.16,1,.3,1),transform .8s cubic-bezier(.16,1,.3,1)}
.device:hover{transform:rotate(0deg) translateY(-6px)!important}

/* index row arrow + underline sweep */
.index-row{position:relative}
.index-row:after{content:"";position:absolute;left:0;bottom:-1px;width:0;height:1px;background:var(--blue);transition:width .45s cubic-bezier(.16,1,.3,1)}
.index-row:hover:after{width:100%}
.index-row .tag{transition:transform .4s cubic-bezier(.16,1,.3,1)}
.index-row:hover .tag{transform:translateX(8px)}

/* leadership rows */
.lead-row h3{transition:color .3s ease,transform .3s cubic-bezier(.16,1,.3,1)}
.lead-row:hover h3{transform:translateX(10px)}

/* nav link underline */
.navlinks a{position:relative;padding-bottom:2px}
.navlinks a:after{content:"";position:absolute;left:0;right:100%;bottom:0;height:1px;background:#fff;transition:right .35s cubic-bezier(.16,1,.3,1)}
.navlinks a:hover:after{right:0}

/* contact arcs pulse */
.arcs path{animation:pulse 4.5s ease-in-out infinite}
.arcs path:nth-child(2){animation-delay:.4s}
.arcs path:nth-child(3){animation-delay:.8s}
@keyframes pulse{0%,100%{opacity:.35;transform:scale(1)}50%{opacity:.9;transform:scale(1.04)}}
.pingus{opacity:0;transform:translateY(24px)}
.contact.in .pingus,.contact.in .gotproject,.contact.in .contact-cta{opacity:1!important;transform:none!important}
.gotproject,.contact-cta{opacity:0;transform:translateY(16px);transition:opacity .8s ease,transform .8s cubic-bezier(.16,1,.3,1)}
.pingus{transition:opacity .9s cubic-bezier(.16,1,.3,1)